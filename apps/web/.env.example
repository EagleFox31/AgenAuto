# AgenAuto local development
# Local Docker binds PostgreSQL to 127.0.0.1 and uses trust auth for development only.
DATABASE_URL=postgresql://agenauto@localhost:5432/agenauto
PAYLOAD_SECRET=<generate-a-long-random-local-secret>
NEXT_PUBLIC_APP_URL=http://localhost:3000
