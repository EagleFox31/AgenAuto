# Next.js app routes

- `(frontend)/` — expérience publique AgenAuto ;
- `(payload)/` — intégration Payload Admin/API.

Les route groups gardent la séparation entre expérience produit et infrastructure Headless Core sans créer deux applications distinctes au MVP.
