# App components

Composants spécifiques à l'application AgenAuto qui ne sont pas encore assez génériques pour vivre dans `packages/ui`.

Règle : lorsqu'un composant devient suffisamment stable et réutilisable, il peut être promu vers `packages/ui` avec documentation et tests adaptés.
