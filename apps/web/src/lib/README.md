# Web shared libraries

Utilitaires techniques partagés par l'application principale.

Exemples futurs :
- accès Payload Local API ;
- helpers de permissions ;
- configuration runtime ;
- validation commune ;
- instrumentation ;
- formatage monétaire / unités.

Les règles métier automobile ne doivent pas être enterrées dans `lib/`.
