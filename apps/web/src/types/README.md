# App types

Types spécifiques à l'application qui ne constituent ni un contrat inter-service ni un invariant automobile réutilisable.

- types métier réutilisables -> `packages/automotive-domain` ;
- contrats aux frontières -> `packages/contracts` ;
- types locaux de présentation/intégration -> ici.
