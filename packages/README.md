# packages

Briques de code réutilisables et découplées des applications déployables.

- `automotive-domain/` — invariants et logique automobile pure ;
- `contracts/` — contrats aux frontières ;
- `ui/` — design system et composants partagés ;
- `config/` — configuration commune.
