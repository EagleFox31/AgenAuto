# tests/e2e

Parcours critiques couverts par Playwright lorsque les features existent.

Cibles MVP :
- search -> vehicle -> compare ;
- compare -> quote/test-drive lead ;
- Payload Admin canonical-data smoke flow ;
- dealer-scoped offer update ;
- dealer lead processing ;
- access-control regression paths.

Les tests doivent utiliser des fixtures déterministes et ne jamais dépendre de credentials de production.
